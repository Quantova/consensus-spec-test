# Point the labs at a Quantova gateway. The labs that need a node skip cleanly
# when this is unset, so npm test passes offline and runs the self verifying
# checks such as the SHA3 256 known answer tests.

QUANTOVA_GATEWAY_URL=https://testnet.quantova.io
